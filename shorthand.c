#include <stdio.h>

    int main(){

        int a , b;

            printf("We have to show the greater value.\n");

            printf("Enter the value of a = ");
            scanf("%d", &a);

            printf("Enter the value of b = ");
            scanf("%d", &b);

            a>=18 || b>60 ? printf("a is true"):printf("b is true");



            return 9;
    }