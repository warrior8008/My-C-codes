#include <stdio.h>

int main(){

        int a , b , SUM;

            printf("Enter the value of a = \n\n");
            scanf("%d", &a);

            printf("Enter the value of b = \n\n");
            scanf("%d", &b);

            SUM = a + b;

            printf("\n\n      The sum of a and b is %d   \n\n", SUM);

    return 0;
}